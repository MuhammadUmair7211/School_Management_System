import Header from "../components/Header";

const AddNewClass = () => {
  return (
    <div>
      <Header
        heading="Add New Class"
        details="Fill in the details to add new class in the system"
        buttonText="Create Class"
      />
    </div>
  );
};

export default AddNewClass;
